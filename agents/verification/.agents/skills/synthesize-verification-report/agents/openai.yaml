interface:
  display_name: "Synthesize Verification Report"
  short_description: "Build final report and strict verdict"
  default_prompt: "Use $synthesize-verification-report to aggregate findings, enforce strict verdict logic, and return only the final JSON directly."

policy:
  allow_implicit_invocation: true
