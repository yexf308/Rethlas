interface:
  display_name: "Recursive Proving"
  short_description: "Add one bounded adversarial critic"
  default_prompt: "Use $recursive-proving to add one context-free adversarial critic before deciding whether wider parallel work is justified."

policy:
  allow_implicit_invocation: true
