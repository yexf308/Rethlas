interface:
  display_name: "Propose Decomposition Plans"
  short_description: "Choose one primary plan and one fallback"
  default_prompt: "Use $propose-subgoal-decomposition-plans to choose one primary decomposition plan and at most one materially different fallback."

policy:
  allow_implicit_invocation: true
