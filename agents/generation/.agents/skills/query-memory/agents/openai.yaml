interface:
  display_name: "Query Memory"
  short_description: "Rehydrate only the memory needed now"
  default_prompt: "Use $query-memory to retrieve one bounded slice of durable memory for the current proof decision."

policy:
  allow_implicit_invocation: true
