interface:
  display_name: "Direct Proving"
  short_description: "Carry one selected plan through coherently"
  default_prompt: "Use $direct-proving to carry one selected plan through in a coherent root reasoning phase and return one consolidated outcome."

policy:
  allow_implicit_invocation: true
