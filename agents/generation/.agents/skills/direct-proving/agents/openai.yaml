interface:
  display_name: "Direct Proving"
  short_description: "Carry one assigned route coherently"
  default_prompt: "Use $direct-proving to carry one assigned route through coherently and return one bounded consolidated report to the root."

policy:
  allow_implicit_invocation: true
