interface:
  display_name: "Identify Key Failures"
  short_description: "Compress root and critic failures"
  default_prompt: "Use $identify-key-failures to compress the blocked root attempt and adversarial-critic report into one next-step synthesis."

policy:
  allow_implicit_invocation: true
